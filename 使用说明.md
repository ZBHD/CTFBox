# CTFBox 使用说明

这份说明面向直接使用 CTFBox 的用户。示例中的 `TARGET_URL` 表示你自己的 CTF 靶场或测试地址；数据库、表和字段名称请以实际回显为准。

## 1. 启动 CTFBox

### 使用 Windows 安装版

1. 从 [GitHub Releases](https://github.com/ZBHD/CTFBox/releases/latest) 下载文件名以 `-setup.exe` 结尾的安装程序。
2. 运行 Setup，并选择安装位置。
3. 等待安装程序复制 CTFBox、SQLmap、SSTImap、汉化资源和 Python 运行时。
4. 安装完成后，双击桌面上的 CTFBox 快捷方式。

安装包内置 WebView2 引导程序。系统缺少 WebView2 Runtime 时，安装过程会自动补充该组件。

### 认识界面

- 左侧 **Web 工具**：SQLmap、SSTImap。
- 左侧 **工具箱**：点击 Crypto 或 Misc，再从展开菜单选择模块。
- 左下角 **设置**：管理全局 Flag 识别和界面主题。
- Web 工具左侧是命令预览、终端和结果区，右侧是参数区。
- “清空”只清理当前工具的当前任务，不会重置主题和 Flag 设置。

## 2. 示例一：使用 SQLmap 检测目标

假设目标地址是 `TARGET_URL`，要测试的参数名是 `id`。

### 第一步：生成检测命令

1. 点击左侧 **SQLmap**。
2. 在顶部选择“原版”或“汉化版”。两者功能相同，区别是帮助和运行提示的语言。
3. 在右侧“目标”分组的“目标 URL”中填写 `TARGET_URL`。
4. 在“注入”分组的“测试参数”中填写 `id`。
5. 在“常规”分组开启“自动确认”。

顶部应生成类似命令：

```powershell
sqlmap.cmd --url TARGET_URL -p id --batch
```

选择汉化版时，命令中会自动加入 `-cn`。

### 第二步：运行并查看回显

1. 核对命令后点击“运行”。
2. 当前命令会出现在终端中，标准输出和错误输出会持续追加。
3. 如果 SQLmap 询问问题，可在终端底部输入答案并发送。
4. 任务结束后，状态会显示“已完成”“失败”或“已停止”。

### 第三步：继续枚举

确认存在注入点后，不要清空任务，继续使用当前 URL 和参数：

1. 打开“枚举”分组，勾选“枚举数据库”。
2. 再次点击“运行”。新的回显会追加为第二条运行记录，第一条记录仍然保留。
3. 从回显中找到数据库名，例如 `TARGET_DB`。
4. 取消“枚举数据库”，在“数据库”中填写 `TARGET_DB`，勾选“枚举数据表”，再次运行。
5. 从回显中找到表名，例如 `TARGET_TABLE`。
6. 填写 `TARGET_TABLE`，勾选“枚举字段列”，再次运行。

对应命令大致如下：

```powershell
sqlmap.cmd --url TARGET_URL -p id --batch --dbs
sqlmap.cmd --url TARGET_URL -p id --batch -D TARGET_DB --tables
sqlmap.cmd --url TARGET_URL -p id --batch -D TARGET_DB -T TARGET_TABLE --columns
```

当前版本会保留并折叠每次运行的回显，但尚未从真实回显中自动填充数据库、表和字段下拉项。请先根据终端结果手动填写。

### 使用 POST、Cookie 或请求文件

如果参数位于 POST 请求体：

1. “目标 URL”填写 `TARGET_URL`。
2. “请求方法”选择 `POST`。
3. “请求数据”填写 `id=1&name=test`。
4. “测试参数”填写 `id`。

如果目标需要登录，在“Cookie”中填写浏览器或抓包工具获得的 Cookie，例如：

```text
session=TARGET_SESSION
```

也可以把完整 HTTP 请求保存为 `request.txt`，在“原始请求文件”中选择该文件。使用请求文件时，通常不再填写目标 URL。

详细参数和命令行案例见 [SQLmap 原版说明](./Original/sqlmap-1.10/使用说明.md) 或 [SQLmap 汉化版说明](./CNversion/sqlmap-1.10/使用说明.md)。

## 3. 示例二：使用 SSTImap 检测模板注入

假设 `TARGET_URL` 的 `name` 参数可能进入模板渲染。

### URL 参数检测

1. 点击左侧 **SSTImap**。
2. 选择原版或汉化版。
3. 在“目标 URL”中填写带注入标记的地址，例如 `TARGET_URL?name=*`。如果 `TARGET_URL` 已经包含参数，请按实际地址把 `*` 放在待测值的位置。
4. 检查命令后点击“运行”。显式使用 `*` 时，不需要再选择“注入点”。

生成的命令类似：

```powershell
sstimap.cmd -u "TARGET_URL?name=*"
```

### POST 请求体检测

如果目标通过表单提交：

1. “目标 URL”填写 `TARGET_URL`。
2. “请求方法”选择 `POST`。
3. “请求数据”填写 `name=*`。
4. “注入点”选择“请求体”。

对应命令：

```powershell
sstimap.cmd -u TARGET_URL -m POST -d "name=*" -P B
```

运行后重点查看回显中的模板引擎、检测技术和可用能力。已知模板引擎时，可以在“检测”分组填写 `jinja2` 等名称，减少无关探测。

详细示例见 [SSTImap 原版说明](./Original/SSTImap-master/使用说明.md) 或 [SSTImap 汉化版说明](./CNversion/SSTImap-master/使用说明.md)。

## 4. 示例三：自动解码并识别 Flag

### 自动解码

1. 点击 **Crypto**，选择“编码转换”。
2. 在输入区粘贴：

```text
5a6d78685a33746a64475a69623368665a4756746233303d
```

3. 不需要点击“处理”，自动解码区域会立即分析输入。

预期结果：

```text
Hex -> Base64
flag{ctfbox_demo}
```

包含 Flag 的结果会排在前面并高亮。自动解码最多递归三层，并过滤明显乱码和重复结果。

### 手动转换

需要明确输出格式时，使用上方的手动控件：

1. 输入 `hello`。
2. 编码选择 `Base64`。
3. 方向选择“编码”。
4. 点击“处理”。

输出应为：

```text
aGVsbG8=
```

“交换输入与输出”可把当前结果移回输入区，继续下一次处理。

### 哈希计算

1. 选择 **Crypto → 哈希识别**。
2. 输入 `hello`。
3. 选择 `SHA-256` 并点击“处理”。

预期输出：

```text
2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824
```

### 循环 XOR

1. 选择 **Crypto → 异或分析**。
2. 输入待处理文本。
3. 在“密钥”中填写循环密钥。
4. 选择 Hex、Base64 或文本输出，再点击“处理”。

XOR 使用同一密钥再次处理可以还原原文。文本输出可能包含不可打印字符，遇到这种情况优先选择 Hex 或 Base64。

## 5. 示例四：配置全局 Flag 识别

1. 点击左下角 **设置**。
2. 进入“Flag 识别”。
3. 保持总开关开启。
4. 在“检测头”中填写：

```text
flag, CTF, TARGET_FLAG
```

多个检测头必须使用英文逗号分隔。默认不区分大小写。

可分别控制：

- 检测终端回显。
- 检测结构化字段。
- 解码疑似 Base64 内容后再检测。
- 是否区分大小写。

“命中后暂停流程”开关已经显示在设置页，但当前版本没有自动执行下一步的流程，因此该选项暂时不会中止后续任务。

## 6. Misc 文件工作区

从 **Misc** 的展开菜单可选择：

- 伪加密：ZIP 本地文件头、中央目录和修复策略。
- LSB 隐写：颜色通道、位平面、RGB/BGR 顺序和结束标记。
- 图片隐写：元数据、颜色通道、尾部附加数据和可打印字符串。
- 音频隐写：波形/频谱、声道和频率范围。

实际使用方式：

1. 选择模块。
2. 拖入文件或点击文件区域选择文件。
3. 确认预览和文件信息。
4. 调整右侧参数。

当前版本只完成文件载入、预览和参数界面。点击分析按钮后会显示“等待分析适配器返回结果”，但不会真正修复 ZIP、提取 LSB 数据或生成音频频谱结果文件。

## 7. 清空任务与切换工具

- 点击“清空”会删除当前工具、当前模块的参数、回显和运行记录。
- 清空 SQLmap 或 SSTImap 后，版本选择会回到“原版”。
- SQLmap、SSTImap、Crypto 和每个 Misc 模块拥有各自的临时任务状态。
- 任务状态只存在于本次应用运行中，关闭应用后不会保存。
- 切换工具不会立即清除先前工具的任务，返回后仍可继续查看。

## 8. 命令行使用

命令行入口面向源码仓库，需要安装 Python 3，并在项目根目录运行。

```powershell
# SQLmap 原版
.\sqlmap.cmd -h

# SQLmap 汉化版
.\sqlmap.cmd -cn -h

# SSTImap 原版
.\sstimap.cmd -h

# SSTImap 汉化版
.\sstimap.cmd -cn -h
```

`-cn` 必须紧跟启动器并位于其他参数之前。

## 9. 常见问题

### 点击运行后一直没有回显

确认使用的是 GitHub Release 中最新的 Setup 安装版。浏览器预览模式没有 Tauri 后端，只能查看界面，不能执行工具。

### 命令运行后要求输入

关闭“自动确认”时，SQLmap 可能询问检测选项。使用终端底部输入框发送答案；需要无人值守运行时开启“自动确认”。

### 上一次运行结果不见了

每次运行记录都可以折叠。检查终端中的历史记录是否处于折叠状态。只有点击“清空”才会移除当前任务的全部记录。

### 汉化版仍出现英文

参数名、数据库名、模板引擎、HTTP 字段、载荷和目标返回内容必须保留英文。汉化范围主要是帮助、状态、提示和错误信息。

### 中文显示乱码

GUI 使用 UTF-8 显示。命令行运行汉化版时建议使用 Windows Terminal；仍有乱码可先执行：

```powershell
chcp 65001
```

### 想重新检测同一个 SQLmap 目标

SQLmap 会复用会话结果。在“常规”分组开启“清除当前会话”，或在命令行增加 `--flush-session`。

### 自动解码没有结果

自动解码只显示可读结果。确认输入没有缺失字符；二进制应按 8 位字节分组，八进制建议使用空格分隔。

## 10. 设置保存范围

- 当前任务不保存，重启应用后清空。
- 主题保存在本机，下次启动自动沿用。
- 当前版本的 Flag 设置只在本次运行中有效。
