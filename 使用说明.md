# CTFBox 使用说明

## 1. 运行要求

### Windows 安装版

1. 从项目的 [GitHub Releases](https://github.com/ZBHD/CTFBox/releases/latest) 下载 `CTFBox_*_x64-setup.exe`。
2. 运行安装程序并选择安装目录，安装完成后桌面会自动生成 CTFBox 快捷方式。
3. 双击桌面快捷方式启动 `CTFBox.exe`。
4. SQLmap、SSTImap、Crypto 与 Misc 均在 CTFBox 窗口中操作。SQLmap 和 SSTImap 使用应用自带的 Python 运行时，不需要另外打开命令窗口。

安装包已嵌入 WebView2 引导程序；系统缺少 WebView2 Runtime 时会在安装过程中补充安装。

### 命令行

- Windows 10/11
- Python 3
- 在 CTFBox 根目录执行命令，或将根目录加入 `PATH`

### 图形界面开发版

- Node.js 与 pnpm
- Rust 工具链
- Windows WebView2 Runtime

启动桌面开发版：

```powershell
cd D:\Projects\CTFBox\gui
pnpm install
pnpm dev
```

启动浏览器预览：

```powershell
cd D:\Projects\CTFBox\gui
pnpm dev:renderer
```

## 2. 界面结构

左侧栏分为三部分：

- **Web 工具**：SQLmap、SSTImap。
- **工具箱**：Crypto、Misc，点击后从下拉菜单选择具体模块。
- **设置**：全局唯一设置页。无论从哪个工具进入，打开的都是同一份设置；Flag 规则和主题对所有工具生效。

每个工具的当前任务只保留在本次运行中。点击“清空”会移除当前工具、当前模块的输入、输出和运行历史，不影响其他工具。

## 3. SQLmap

1. 从左侧选择 **SQLmap**。
2. 在顶部选择“原版”或“汉化版”。
3. 在右侧参数区填写目标地址，并按目标、请求、注入、检测、技术、枚举、性能和常规分组配置参数。
4. 顶部命令预览会随参数持续更新，执行下一条命令时不会清除此前回显。
5. 运行记录可折叠；数据库、数据表和字段结果会进入对应的联动入口。

点击“运行”后，CTFBox 会在后台启动对应版本并将 stdout、stderr 实时显示在内置终端。运行中可通过终端底部输入框回答工具提示，也可点击顶部“停止”终止当前任务。任务退出后会自动显示“已完成”“失败”或“已停止”。

命令行入口：

```powershell
# 原版
.\sqlmap.cmd -u "TARGET_URL" --batch

# 汉化版
.\sqlmap.cmd -cn -u "TARGET_URL" --batch
```

更多参数见 [SQLmap 原版说明](./Original/sqlmap-1.10/使用说明.md) 和 [SQLmap 汉化版说明](./CNversion/sqlmap-1.10/使用说明.md)。

## 4. SSTImap

1. 从左侧选择 **SSTImap**。
2. 选择原版或汉化版。
3. 按目标、请求、爬虫、检测、载荷和常规分组填写参数。
4. 核对顶部命令预览后点击“运行”，输出会持续进入内置终端。
5. 需要交互时在终端底部输入内容；任务退出后状态会自动更新。

命令行入口：

```powershell
# 原版
.\sstimap.cmd -u "TARGET_URL"

# 汉化版
.\sstimap.cmd -cn -u "TARGET_URL"
```

更多参数见 [SSTImap 原版说明](./Original/SSTImap-master/使用说明.md) 和 [SSTImap 汉化版说明](./CNversion/SSTImap-master/使用说明.md)。

## 5. Crypto

### 5.1 编码转换

支持以下编码：

- Base64
- Base32
- Base58
- Base85
- Ascii85
- Hex
- URL
- HTML 实体
- Unicode 转义
- 二进制
- 八进制

手动转换步骤：

1. 进入 **Crypto → 编码转换**。
2. 在左侧输入文本，或通过文件按钮读取文本文件。
3. 选择编码和“编码/解码”方向。
4. 点击“处理”，结果显示在右侧。
5. 可复制结果，或交换输入与输出继续处理。

### 5.2 自动解码

输入内容后无需点击处理，底部“自动解码”区域会立即：

1. 尝试所有支持的可逆编码。
2. 对可读结果继续递归解码，最多三层。
3. 去除重复结果并过滤明显乱码。
4. 显示完整解码路径，例如 `Hex → Base64`。
5. 将包含全局 Flag 检测头的结果置顶，并高亮具体 Flag 文本。

自动结果用于快速筛选。需要固定编码输出时，仍使用上方手动转换控件。

### 5.3 哈希计算

进入 **Crypto → 哈希识别**，输入内容并选择 SHA-1、SHA-256、SHA-384 或 SHA-512，然后点击“处理”。输出为十六进制摘要。

### 5.4 异或分析

进入 **Crypto → 异或分析**，输入原文和循环密钥，选择 Hex、Base64 或文本输出格式，然后点击“处理”。

## 6. Misc

Misc 不使用模拟终端，而是为文件类任务提供专用工作区：

- **伪加密**：ZIP 标记检查与修复策略选择。
- **LSB 隐写**：颜色通道、位平面与提取顺序配置。
- **图片隐写**：元数据、颜色通道、尾部附加数据和字符串检查。
- **音频隐写**：波形、频谱、声道和频率范围配置。

当前版本已提供文件载入、预览和分析参数界面，具体提取执行器将随工具适配器继续接入。

## 7. 全局 Flag 识别

进入 **设置 → Flag 识别**：

- 开启或关闭全局识别。
- 使用英文逗号配置多个检测头，例如 `flag, CTF`。
- 分别控制终端回显、结构化结果和 Base64 编码检测。
- 选择是否区分大小写。

识别结果只高亮具体 Flag，不添加额外命中文字。Crypto 自动解码会使用同一套检测头，并把命中结果置顶。

## 8. 亮色与暗色主题

进入 **设置 → 外观**，选择“暗色”或“亮色”。设置页是全局设置页，主题会立即应用到全部工具，并保存到本机；下次启动自动沿用。

## 9. 清空当前任务

每个工具顶部均提供“清空”按钮。清空只作用于当前工具和当前模块，适合完成一个目标后快速开始下一项任务。主题与全局 Flag 设置不会被清空。

## 10. 常见问题

### GUI 显示“浏览器预览模式”

使用 `pnpm dev:renderer` 时没有 Tauri 后端，这是正常状态。需要桌面后端时运行 `pnpm dev`。

### Web 工具一直显示“运行中”或没有回显

确认使用的是最新安装包。当前版本通过 Tauri Channel 接收实时输出，并在进程退出时自动更新状态；旧安装包需要卸载或重新安装后再测试。

### 汉化版命令没有生效

确认命令中包含 `-cn`，并从 CTFBox 根目录调用统一启动器：

```powershell
.\sqlmap.cmd -cn -h
.\sstimap.cmd -cn -h
```

### 自动解码没有结果

自动解码只保留有效且可读的文本。二进制输入需要按 8 位字节分组，八进制输入建议使用空格分隔，编码层数上限为三层。

### 重置主题

进入“设置 → 外观”重新选择即可。主题保存在浏览器或 WebView 的本地存储中，不会写入任务文件。
